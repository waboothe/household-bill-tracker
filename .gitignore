node_modules
# dist is intentionally committed so the NAS can serve the pre-built
# bundle without needing Node/npm. Rebuild locally with `npm run build`
# before committing changes.
.DS_Store
.vite
*.log
.env
.env.*

# Python backend
backend/.venv
backend/__pycache__
backend/**/__pycache__
backend/*.db
backend/*.db-journal
*.pyc
