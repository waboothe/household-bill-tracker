[project]
name = "bill-tracker-api"
version = "0.1.0"
description = "FastAPI + SQLite backend for the Household Bill Tracker."
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.115",
    "uvicorn[standard]>=0.32",
    "pydantic>=2.9",
]
